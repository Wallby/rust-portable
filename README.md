# How to use

Download (or clone) the entire repository. Double click `download.bat`. That's it.

To add it to the path environment variable (system or user), add this folder (containing cargo.bat and rustup.bat) to the PATH.
If you add it to the path, also remove download.bat now.