# How to use

Download (or clone) the entire repository. Double click `download.bat`. That's it.

To add the rust programs to the path environment variable (system or user), add the generated bin folder.